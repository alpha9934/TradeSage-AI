vector_db:
  index_name: "trading-bot"

retriever:
  top_k: 2        # was 3, reduce to 2 or even 1
  score_threshold: 0.5

embedding_model:
  model_name: "gemini-embedding-001"
  dimension: 3072

llm:
  google:
    provider: "google"
    model_name: "gemini-1.5-pro"
 
  groq:
    provider: "groq"
    model_name: "llama-3.1-8b-instant" # smaller model, higher free tier limits # Updated to fix the 400 error

tools:
  tavily:
    max_results: 5